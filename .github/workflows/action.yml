name: Build and deploy action
on: [push]
jobs:
  build_and_push:
    runs-on: ubuntu-latest
    steps:
      - name: clone code
        uses: actions/checkout@v2
      - name: docker build
      - run: docker build -t mikepodolskiy/skypro_lesson26:$GITHUB_REF_NAME-$GITHUB_RUN_ID .

